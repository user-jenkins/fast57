apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: fast57-ingress
spec:
  rules:
  - host: fast57.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: fast57-service
            port:
              number: 80
